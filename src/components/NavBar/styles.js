import {makeStyles} from "@material-ui/core/styles";

export const useStyles = makeStyles({
    navContainer: {
        margin: `0 auto`,
        '& button': {
            color: '#fff'
        }
    }
})

