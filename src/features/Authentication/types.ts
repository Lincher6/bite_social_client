export type SignUpFromType = {
    signUp: Function,
    loginError: string,
    isLoading: boolean
}

export type LoginFromType = {
    login: Function,
    loginError: string,
    isLoading: boolean
}